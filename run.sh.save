#!/bin/bash

source ~/venvs/Flask/bin/activate
export FLASK_APP=cam.py
flask run lush block if this flush is occurring prematurely) (exceptions.TypeError) SQLite DateTime type only accepts Python datetime and date objects as input. [SQL: u'UPDATE post SET timesta
